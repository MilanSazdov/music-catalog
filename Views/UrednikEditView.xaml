<Window x:Class="MusicCatalog.Views.UrednikEditView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:local="clr-namespace:MusicCatalog.Views"
        xmlns:utils="clr-namespace:MusicCatalog.Utils"
        mc:Ignorable="d"
        Title="UrednikEditView"
                
        AllowsTransparency="True"
        Background="Transparent"
        WindowStyle="None"
        WindowStartupLocation="CenterOwner"
        SizeToContent="WidthAndHeight">

    <Window.Resources>
        <Style x:Key="ChipRemoveButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource HintBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontSize" Value="10"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Content" Value="X"/>
            <Setter Property="Margin" Value="4,0,0,0"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="2">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Foreground" Value="{StaticResource PrimaryBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid MouseLeftButtonDown="Window_MouseLeftButtonDown">
        <Border Background="{StaticResource SurfaceBrush}" CornerRadius="8"
			    Padding="40"
			    Margin="20"
			    MaxWidth="500" Effect="{StaticResource ShadowEffect}">

            <StackPanel>
                <TextBlock Text="{Binding Title, FallbackValue='Urednik'}" 
                           Foreground="{StaticResource PrimaryBrush}"
                           FontSize="24" FontWeight="Bold" 
                           HorizontalAlignment="Center"
                           Margin="0,0,0,24"/>

                <TextBlock Text="Ime" Style="{StaticResource FormLabel}"/>
                <TextBox
                    Text="{Binding Ime, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}"
                    Style="{StaticResource ModernTextBox}"
                    Margin="0,0,0,20"/>

                <TextBlock Text="Prezime" Style="{StaticResource FormLabel}"/>
                <TextBox
                    Text="{Binding Prezime, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}"
                    Style="{StaticResource ModernTextBox}"
                    Margin="0,0,0,20"/>

                <TextBlock Text="Email adresa" Style="{StaticResource FormLabel}"/>
                <TextBox
                    Text="{Binding Email, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}"
                    Style="{StaticResource ModernTextBox}"
                    Margin="0,0,0,20"/>

                <TextBlock>
                    <TextBlock.Style>
                        <Style TargetType="TextBlock" BasedOn="{StaticResource FormLabel}">
                            <Setter Property="Text" Value="Nova Lozinka (ostavi prazno da zadržiš staru)"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsEditing}" Value="False">
                                    <Setter Property="Text" Value="Lozinka"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>

                <PasswordBox
                    utils:PasswordHelper.Attach="True"
                    utils:PasswordHelper.Password="{Binding Password, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}"
                    Style="{StaticResource ModernPasswordBox}"
                    Margin="0,0,0,20"/>

                <TextBlock>
                    <TextBlock.Style>
                        <Style TargetType="TextBlock" BasedOn="{StaticResource FormLabel}">
                            <Setter Property="Text" Value="Potvrdi Novu Lozinku"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsEditing}" Value="False">
                                    <Setter Property="Text" Value="Potvrdi Lozinku"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>

                <PasswordBox
                    utils:PasswordHelper.Attach="True"
                    utils:PasswordHelper.Password="{Binding ConfirmPassword, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}"
                    Style="{StaticResource ModernPasswordBox}"
                    Margin="0,0,0,20"/>


                <TextBlock Text="Specijalizacija (Žanrovi)" Style="{StaticResource FormLabel}"/>
                <StackPanel Orientation="Horizontal">
                    <ComboBox ItemsSource="{Binding SviZanrovi}"
                              SelectedItem="{Binding NewZanr}" 
                              Width="332"
                              DisplayMemberPath="Naziv"/>

                    <Button Content="Dodaj" 
                            Command="{Binding AddZanrCommand}" 
                            Width="80" 
                            Style="{StaticResource PrimaryButton}"
                            Margin="8,0,0,0"/>
                </StackPanel>

                <Border BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="4" 
                        MinHeight="80" MaxHeight="120" Margin="0,12,0,0"
                        Background="{StaticResource WindowBackgroundBrush}">
                    <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="5">
                        <ItemsControl ItemsSource="{Binding Specijalizacija}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <WrapPanel Orientation="Horizontal" />
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="{StaticResource PrimaryBrush}" CornerRadius="4" Margin="3">
                                        <StackPanel Orientation="Horizontal" Margin="8,4">
                                            <TextBlock Text="{Binding Naziv}" 
                                                       Foreground="{StaticResource ButtonTextBrush}" 
                                                       VerticalAlignment="Center"/>
                                            <Button Style="{StaticResource ChipRemoveButton}"
                                                      Foreground="{StaticResource ButtonTextBrush}"
                                                    Command="{Binding DataContext.RemoveZanrCommand, 
                                                                RelativeSource={RelativeSource AncestorType=Window}}"
                                                    CommandParameter="{Binding}"/>
                                        </StackPanel>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </Border>


                <TextBlock
                    Text="{Binding ErrorMessage}"
                    Foreground="{StaticResource ErrorBrush}"
                    TextWrapping="Wrap"
                    Margin="0,12,0,12"
                    HorizontalAlignment="Center"
                    Visibility="{Binding ErrorMessage, Converter={StaticResource BooleanToVisibilityConverter}, FallbackValue=Collapsed}"/>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Stretch" Margin="0,16,0,0">
                    <Button
                        Content="Potvrdi"
                        Command="{Binding ConfirmCommand}"
                        Style="{StaticResource PrimaryButton}"
                        Margin="0,0,8,0"
                        Width="202"/>

                    <Button
                        Content="Otkaži"
                        Command="{Binding CancelCommand}"
                        Style="{StaticResource PrimaryButton}"
                        Margin="8,0,0,0"
                        Background="{StaticResource ErrorBrush}"
                        Width="202"/>
                </StackPanel>

            </StackPanel>
        </Border>
    </Grid>
</Window>