<UserControl x:Class="MusicCatalog.Views.RecenzijaEditView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:utils="clr-namespace:MusicCatalog.Utils"
             mc:Ignorable="d" 
             d:DesignHeight="360" d:DesignWidth="420"
             Background="{StaticResource SurfaceBrush}">

    <UserControl.Resources>
        <utils:BooleanToVisibilityConverter x:Key="BoolToVis"/>

        <Style x:Key="CloseButtonX" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource HintBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Content" Value="X"/>
            <Setter Property="Margin" Value="0,10,10,0"/>
            <Setter Property="VerticalAlignment" Value="Top"/>
            <Setter Property="HorizontalAlignment" Value="Right"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="2" Padding="8,4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Foreground" Value="{StaticResource PrimaryBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid>
        <StackPanel Margin="30">

            <TextBlock Text="{Binding DeloNaziv}" 
                       FontWeight="Bold" 
                       FontSize="20" 
                       Foreground="{StaticResource PrimaryBrush}"
                       TextTrimming="CharacterEllipsis"
                       Margin="0,0,0,16"/>

            <StackPanel Orientation="Vertical">

                <StackPanel Visibility="{Binding IsCreate, Converter={StaticResource BoolToVis}}">
                    <TextBlock Text="Ocena (1-5)" Style="{StaticResource FormLabel}"/>
                    <TextBox Width="80"
                             HorizontalAlignment="Left"
                             Style="{StaticResource ModernTextBox}"
                             Text="{Binding Vrednost, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                </StackPanel>

                <StackPanel Visibility="{Binding IsCreate, Converter={StaticResource BoolToVis}, ConverterParameter=inverse}">
                    <TextBlock Text="Nova Ocena (1-5)" Style="{StaticResource FormLabel}"/>
                    <TextBox Width="80"
                             HorizontalAlignment="Left"
                             Style="{StaticResource ModernTextBox}"
                             Text="{Binding NovaOcenaVrednost, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                </StackPanel>

                <TextBlock Text="Komentar (opciono)" Style="{StaticResource FormLabel}" Margin="0,15,0,4"/>
                <TextBox Text="{Binding Opis, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                         Style="{StaticResource ModernTextBox}"
                         Height="80"
                         AcceptsReturn="True"
                         TextWrapping="Wrap"
                         VerticalScrollBarVisibility="Auto"/>
            </StackPanel>

            <StackPanel Grid.Row="3" Orientation="Vertical" HorizontalAlignment="Stretch" Margin="0,20,0,0">

                <Button Content="Sačuvaj Ocenu"
                        Command="{Binding SaveCreateCommand}"
                        Style="{StaticResource PrimaryButton}"
                        Visibility="{Binding IsCreate, Converter={StaticResource BoolToVis}}"/>

                <Button Content="Pošalji Zahtev za Izmenu"
                        Command="{Binding PosaljiZahtevCommand}"
                        Style="{StaticResource PrimaryButton}"
                        Visibility="{Binding IsCreate, Converter={StaticResource BoolToVis}, ConverterParameter=inverse}"/>

                <Button Content="Zatraži Brisanje Recenzije"
                        Style="{StaticResource PrimaryButton}"
                        Background="{StaticResource ErrorBrush}"
                        Command="{Binding IzbrisiRecenzijuCommand}"
                        Visibility="{Binding IsCreate, Converter={StaticResource BoolToVis}, ConverterParameter=inverse}"
                        Margin="0,8,0,0"/>
            </StackPanel>

        </StackPanel>

        <Button Style="{StaticResource CloseButtonX}"
                Command="{Binding CloseCommand}"/>

    </Grid>
</UserControl>